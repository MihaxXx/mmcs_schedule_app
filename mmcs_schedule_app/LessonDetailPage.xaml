<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:mmcs_schedule_app"
             x:Class="mmcs_schedule_app.LessonDetailPage"
             x:DataType="local:LessonDetailPage"
             BackgroundColor="Transparent">
    <Grid>
        <!-- Semi-transparent background -->
        <BoxView 
            BackgroundColor="Transparent">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnBackgroundTapped"/>
            </BoxView.GestureRecognizers>
        </BoxView>
        
        <!-- Popup content -->
        <Border VerticalOptions="End"
                Margin="0"
                Padding="0"
                Stroke="{AppThemeBinding Light=LightGray, Dark=Gray}"
                BackgroundColor="{AppThemeBinding Light=#E6FFFFFF, Dark=#E61E1E1E}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="20,20,0,0"/>
            </Border.StrokeShape>
            <Border.GestureRecognizers>
                <SwipeGestureRecognizer Direction="Down" Swiped="OnSwipeDown"/>
            </Border.GestureRecognizers>
            <VerticalStackLayout Spacing="0">
                <!-- Handle bar -->
                <BoxView HeightRequest="4"
                         WidthRequest="40"
                         CornerRadius="2"
                         Color="Gray"
                         HorizontalOptions="Center"
                         Margin="0,10,0,15"/>
                
                <ListView ItemsSource="{Binding Items}"
                          MaximumHeightRequest="600"
                          SelectionMode="None"
                          SeparatorVisibility="None"
                          HasUnevenRows="True">
                    <ListView.Header>
                        <VerticalStackLayout Padding="20,0,20,10" Spacing="10">
                            <!-- Merged lesson details -->
                            <Label HorizontalTextAlignment="Center" LineBreakMode="WordWrap">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <!-- Discipline name -->
                                        <Span Text="{Binding DisciplineName}"
                                              FontSize="20"
                                              FontAttributes="Bold"/>
                                        <Span Text="&#x0a;"/>
                                        
                                        <!-- Weekday -->
                                        <Span Text="{Binding Weekday}"
                                              FontSize="16"/>
                                        <Span Text="&#x0a;"/>
                                        
                                        <!-- Timeslot -->
                                        <Span Text="{Binding Timeslot}"
                                              FontSize="16"/>
                                        <Span Text="{Binding WeekTypeWithNewline}"
                                              FontSize="14"
                                              FontAttributes="Italic"/>
                                        <Span Text="{Binding RoomInfoWithNewline}"
                                              FontSize="14"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            
                            <!-- Teachers/Groups label -->
                            <Label Text="{Binding ListLabel}"
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   Margin="0,5,0,0"/>
                        </VerticalStackLayout>
                    </ListView.Header>
                    
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:LessonItemInfo">
                            <ViewCell>
                                <Border Padding="5,5,5,5"
                                        Margin="20,0,20,5"
                                        Stroke="Transparent"
                                        BackgroundColor="Transparent">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type local:LessonDetailPage}}, Path=ItemTappedCommand}"
                                                             CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                    <HorizontalStackLayout Spacing="8">
                                        <!-- Bullet point -->
                                        <Label Text="•"
                                               FontSize="16"
                                               VerticalOptions="Start"/>
                                        
                                        <VerticalStackLayout Spacing="2"
                                                            HorizontalOptions="FillAndExpand">
                                            <Label Text="{Binding MainText}"
                                                   FontSize="16"/>
                                            <Label Text="{Binding SubText}"
                                                   FontSize="14"
                                                   TextColor="Gray"
                                                   IsVisible="{Binding HasSubText}"/>
                                        </VerticalStackLayout>
                                    </HorizontalStackLayout>
                                </Border>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
